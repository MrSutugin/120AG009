<!-- ======================= Partial shared/user/_user_info START -->
<%= render 'shared/user/user_info', user: current_user %>
<!-- ======================= Partial shared/user/_user_info END -->


<!-- ======================= Page content START -->
<section class="pt-0">
  <div class="container">
    <div class="row">
      <!-- Left sidebar START -->
      <div class="col-lg-4">
        <!-- Sidebar body -->
        <div class="offcanvas-body p-0">
          <div class="bg-light  rounded-3 pb-0 p-3 w-100 mb-4">
            <!-- ======================= Partial shared/user/_dashboard_menu START -->
            <%= render 'shared/user/dashboard_menu', user: current_user %>
            <!-- ======================= Partial shared/user/_dashboard_menu END -->
          </div>
        </div>
      </div>
      <!-- Left sidebar END -->
      <!-- Main content START -->
      <div class="col-lg-8">
        <div class="card border bg-transparent rounded-3 mb-0">
          <!-- Card header -->
          <div class="card-header bg-transparent border-bottom">
            <h4 class="card-header-title mb-0">Мое товарищество</h4>
          </div>
          <!-- Card body -->
          <div class="card-body">
            <!-- Search START -->
            <div class="row g-4 align-items-center justify-content-between mb-4">
              <div class="col-md-8">
                <form class="rounded position-relative">
                  <input class="form-control pe-5 bg-transparent" type="Найти" placeholder="Найти" aria-label="Найти">
                  <button class="btn bg-transparent px-2 py-0 position-absolute top-50 end-0 translate-middle-y" type="submit">
                    <i class="fas fa-search fs-6 "></i>
                  </button>
                </form>
              </div>
              <div class="col-md-4">
                <%= link_to new_snt_path, class: "btn w-100 btn-primary mb-0" do %>
                  Создать
                <% end %>
              </div>
            </div>
            <!-- Search END -->
            <div class="">
              <h4 class=" h5 ">Мои товарищества</h4>
            </div>
            <div class="row g-4" id="snts">
              <% current_user.snts.each do |snt| %>
                <%= render snt %>
              <% end %>
            </div>
          </div>
        </div>
      </div>
      <!-- Main content END -->
    </div>
    <!-- Row END -->
  </div>
</section>
<!-- ======================= Page content END -->