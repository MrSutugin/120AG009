<%= form_with(model: snt) do |form| %>
  <% if snt.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(snt.errors.count, "error") %> prohibited this snt from being saved:</h2>

      <ul>
        <% snt.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row g-4">
    <div class="col-md-12">
      <%= form.label :name, "Название", class: "form-label" %>
      <%= form.text_field :name, class: "form-control" %>
    </div>


    <div class="col-md-12">
      <%= form.label :address, "Адрес", class: "form-label" %>
      <%= form.text_field :address, class: "form-control" %>
    </div>

    <div class="col-md-12">
      <%= form.label :description, "Описание", class: "form-label" %>
      <%= form.text_area :description, class: "form-control" %>
    </div>


    <div class="col-md-12">
      <%= form.submit "Сохранить", class: "btn btn-success mb-2 mb-sm-0" %>
    </div>
  </div>

<% end %>
